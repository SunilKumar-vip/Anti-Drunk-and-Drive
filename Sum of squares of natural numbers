//Program to find sum of squares of natural numbers

#include<stdio.h>
void main()
{
    int n,i,sum=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
      sum=sum+(i*i);    //precedance 1st multiplication will done then addition its simple :)
    }
    printf("%d",sum);
}
